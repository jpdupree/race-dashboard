{
  "auth": {
    "proxyUrl": null,
    "_comment": "To enable username/password sign-in for crew, deploy worker/ to Cloudflare and set proxyUrl to the worker's public URL (e.g. https://api.example.com/race-dashboard). With proxyUrl null, the hub uses direct PAT mode."
  }
}
